// const Note = require('./note');

// const getNoteById = id => {
//   return Note.findById(id).exec();
// };

// const getAllNotes = () => {
//   return Note.find({}).exec();
// };

// const createNote = noteDetails => {
//   return Note.create(noteDetails);
// };

// const removeNodeById = id => {
//   return Note.findByIdAndRemove(id).exec();
// };

// const updateNoteById = (id, update) => {
//   // Third parameter means returns the update object
//   return Note.findByIdAndUpdate(id, update, { new: true }).exec();
// };

// module.exports = {
//   getNoteById,
//   getAllNotes,
//   createNote,
//   removeNodeById,
//   updateNoteById,
// };
